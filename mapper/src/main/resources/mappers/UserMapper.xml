<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cys.hfparking.dao.UserMapper">
  <resultMap id="BaseResultMap" type="com.cys.hfparking.entity.User">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="access" jdbcType="INTEGER" property="access" />
  </resultMap>
  <select id="findUserByKw" resultMap="BaseResultMap">
    select
    id,
    username,
    password,
    access
    from user
    <where>
        and access!=1
      <if test="username!=''and username!=null and username!='%%'">
        and username like #{username}
      </if>
    </where>
    limit #{start},#{limit}
  </select>
  <delete id="delSel">
    delete from user where id in
    <foreach collection="ids" item="id" open="(" close=")" separator=",">
      #{id}
    </foreach>
  </delete>

  <insert id="addExcelUsers">
    insert into user (
    username,
    password,
    access
    )
    values
    <foreach collection="users" item="user" index= "index" separator =",">
      (
      #{user.username},
      #{user.password},
      #{user.access}
      )
    </foreach>
  </insert>
  <select id="findUserById" resultMap="BaseResultMap">
    select
    id,
    username,
    password,
    access
    from user
    where id=#{id}
  </select>
</mapper>